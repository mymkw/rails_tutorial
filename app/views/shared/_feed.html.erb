<% if @feed_items.any? %>
  <ol class="microposts">
    <%# Railsマジック↓ @feed_itemsはMicropostsクラスだから_micropost.html.erbを呼び出す。%>
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>